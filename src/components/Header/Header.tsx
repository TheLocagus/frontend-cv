import React from 'react';
import { Banner } from './Banner/Banner';

import './Header.scss';

export const Header = () => {

    return (
        <header className="header">
            <Banner/>
        </header>
    )
}